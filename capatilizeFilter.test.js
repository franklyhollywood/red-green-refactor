const capatilizeFilter = require('./CapatilizeFilter');

describe('capatilizeFilter', () => {
    it('capitalizes all strings in array and filter out any string that start with the letter F/f', () => {
        const noCap = ['a', 'b', 'c']
    }


})